with Banque, Commerce, Des, Des_Cases, Files_Cartes, Joueur, Liste_Proprietes, Un_Plateau;
use Banque, Commerce, Des, Des_Cases, Files_Cartes, Joueur, Liste_Proprietes, Un_Plateau;

procedure Main is
   
   subtype Un_Nb_Joueurs is Positive range 1..6;
   Nb_Joueurs : Un_Nb_Joueurs;
   Cond1, Cond2, Cond3 : Boolean;
   
begin
   
   while Cond loop
      
      for I in Joueurs'Range loop
	 
         if Cond1 then
	    null;
	 else
	    null;
	 end if;
	 
	 if Cond2 then
	    null;
	 else
	    null;
	 end if;
	 
	 if Cond3 then
	    null;
	 else
	    null;
	 end if;
	 
      end loop;
      
   end loop;
   
end Main;
